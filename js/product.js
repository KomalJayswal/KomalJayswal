// product.js for Product Detail Page functionality

function addToCart(productId) {
    console.log(`Product ${productId} added to cart`);
    // Add functionality to add the product to the shopping cart
}

document.addEventListener('DOMContentLoaded', () => {
    console.log('Product Page Loaded');
    // Add any additional JavaScript functionality for the Product Detail Page here
});
